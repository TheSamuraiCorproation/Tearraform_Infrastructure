input {
  beats { port => 5044 }
}

filter {
  # you can load rules here (example: grok or xml parsing)
}

output {
  elasticsearch {
    hosts => ["http://elasticsearch:9200"]
    index => "logs-%{+YYYY.MM.dd}"
  }
}
