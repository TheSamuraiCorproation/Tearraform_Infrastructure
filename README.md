# Cybersecurity Training Platform — Infrastructure

This repository contains the Infrastructure as Code (IaC) for the DOJO cybersecurity platform. It provisions vulnerable virtual machines (labs) for users to practice real-world attack scenarios in an isolated AWS environment depending on their use case.

---

## Overview

When a user selects their security tools and attacks on the platform, the following automated workflow is triggered:

```
User selects attacks & tools on platform
        ↓
Backend generates 1st JSON → uploaded to S3 (thesamuraibucket)
        ↓
Jenkins triggers → Terraform provisions EC2 (vulnerable VM)
        ↓
Ansible installs security tools on the VM
        ↓
Jenkins generates 2nd JSON → uploaded to S3 (ip-test-bucket-1774355639)
        ↓
Kali Linux VM reads 2nd JSON → executes attacks on vulnerable VM
```

---

## Repository Structure

```
.
├── main.tf                        # Root module — reads payload, provisions EC2 or EKS
├── variables.tf                   # Root-level input variables
├── modules/
│   ├── ec2/
│   │   ├── ec2.tf                 # EC2 instance resource
│   │   ├── sg_vulnerable_vm.tf    # Dynamic security group for the vulnerable VM
│   │   ├── variables.tf           # EC2 module input variables
│   │   └── outputs.tf             # EC2 module outputs
│   └── eks/
│       ├── main.tf                # EKS cluster resource
│       └── variables.tf           # EKS module input variables
├── ansible/
│   ├── install_tools.yml          # Main Ansible playbook — installs tools on the VM
│   └── tasks/                     # Individual tool deployment task files
├── jenkins/
│   └── Jenkinsfile                # CI/CD pipeline definition
├── backends/
│   ├── backend-ec2.tf             # S3 backend config for EC2 deployments
│   └── backend-eks.tf             # S3 backend config for EKS deployments
└── payloads/
    └── ec2-sample.json            # Sample payload JSON for testing
```

---

## How It Works

### 1st JSON — Lab Configuration (trigger payload)

Uploaded to S3 to trigger the Jenkins pipeline. Contains everything needed to provision the lab.

```json
{
  "file_name": "test_lab_1234567890",
  "user_name": "Mohamed Hedi Nasri",
  "client_email": "mnasri@thesamurai.com",
  "service_type": "ec2",
  "log_output": "internal",
  "logs": {
    "internal": true
  },
  "attacks": [
    { "type": "network", "name": "recon-full-tcp-sweep" },
    { "type": "web",     "name": "web-enum-ffuf" },
    { "type": "os",      "name": "os-ssh-bruteforce-demo" },
    { "type": "ai",      "name": "bert-adversarial-unicode" }
  ],
  "instances": {
    "DEMO-vm": {
      "name": "NICE-VM",
      "ami": "ami-0ac35e575a87fd5d5",
      "instance_type": "t3.xlarge",
      "security_groups": ["sg-0d1e97a685dc65bd9"],
      "subnet_id": "subnet-0e40cc88942302f2e",
      "tools_to_install": [
        {
          "name": "suricata",
          "ecr_repo_url": "430118831270.dkr.ecr.eu-central-1.amazonaws.com/suricata-custom",
          "image_tag": "latest",
          "run_args": "--cap-add=NET_ADMIN --cap-add=NET_RAW --network host -v /var/log/suricata:/var/log/suricata"
        }
      ],
      "tags": { "Owner": "Mohamed Hedi Nasri" }
    }
  }
}
```

### 2nd JSON — Attack Config (consumed by Kali)

Generated by Jenkins after provisioning and uploaded to the output S3 bucket. The Kali machine reads this to know what attacks to run and where.

```json
{
  "lab_id": "lab-i-0xxxxxxxxxxxxxxxxx-42",
  "user_name": "Mohamed Hedi Nasri",
  "client_email": "mnasri@thesamurai.com",
  "instance_name": "DEMO-vm",
  "instance_id": "i-0xxxxxxxxxxxxxxxxx",
  "ip_address": "3.xx.xx.xx",
  "attacks": [...],
  "tools": [...],
  "log_output": "internal",
  "logs": { "internal": true },
  "vm_username": "ubuntu",
  "vm_password": "ubuntu"
}
```

---

## Attack Types & Port Mapping

There are four fixed attack categories. The security group opens ports based on which types the user selected.

| Type | Ports Opened | Notes |
|---|---|---|
| `ai` | 8080, 8001, 5001 | Gemini sandbox, Crop ML model, BERT ML model |
| `os` | 22 | SSH brute force and persistence attacks |
| `network` | 22, 80, 443 | Nmap scans and TCP sweeps |
| `web` | 3000 | Vulnerable web application |

The following ports are **always open** on every lab regardless of attack selection:

| Port | Protocol | Service |
|---|---|---|
| 22 | TCP | SSH |
| 80 | TCP | HTTP |
| 443 | TCP | HTTPS |
| 3306 | TCP | MySQL |
| 53 | TCP/UDP | DNS |
| 25 | TCP | SMTP |
| 3389 | TCP | RDP |
| 514 | UDP | Syslog |

All ingress rules are scoped exclusively to the Kali machine's private IP.

---

## Available Attacks

### AI
| Name | Description |
|---|---|
| `bert-adversarial-unicode` | BERT Adversarial Unicode Attack |
| `bert-membership-inference` | BERT Membership Inference |
| `bert-model-extraction` | BERT Model Extraction (Surrogate Training) |
| `crop-backdoor-trigger-discovery` | Backdoor Trigger Discovery with Heatmaps (LightGBM) |
| `crop-ownership-forgery` | Ownership Forgery (Watermark/Signature Bypass) |
| `gemini-prompt-injection` | LLM Prompt Injection (Gemini) |

### OS
| Name | Description |
|---|---|
| `os-ssh-bruteforce-demo` | SSH Brute-Force Demo with Known-Good Login |
| `os-lazagne-local-dump` | Local Credential Discovery (LaZagne) |
| `os-post-exploit-persistence-ssh-cron` | Post-Exploitation Persistence (SSH Key + @reboot Cron) |

### Network
| Name | Description |
|---|---|
| `os-detection-nmap` | Nmap OS Detection (Guess) |
| `recon-full-tcp-sweep` | Full TCP Port Sweep (1-65535) |
| `recon-quick-top-ports` | Quick Scan (Top 1000 Ports) |
| `service-and-version-detection` | Service and Version Detection (Default NSE) |

### Web
| Name | Description |
|---|---|
| `web-enum-ffuf` | Web Directory Enumeration (ffuf) |

---

## Infrastructure Details

### Terraform State Isolation
Each pipeline run gets its own isolated Terraform state file in S3:
```
s3://thesamuraibucket/labs/{BUILD_ID}/terraform.tfstate
```
This allows multiple labs to run simultaneously without interfering with each other.

### Dynamic Security Group
The vulnerable VM's security group (`sg_vulnerable_vm.tf`) is created fresh per deployment. The SG name includes a hash of the selected attack types, so a different attack selection always produces a new SG with the correct ports. The instance and SG lifecycle are tied together via `replace_triggered_by`.

### Lab Identification
Each lab is assigned a unique `lab_id` in the format `lab-{instance_id}-{build_id}`. This is included in the 2nd JSON and attached as a tag on the EC2 instance for easy identification in AWS.

### Kali Machine
The Kali Linux attacking machine (`i-0c2181477f34261e3`) is a pre-existing AWS instance not managed by Terraform. Its private IP is fetched at plan time via a data source and used to scope all security group ingress rules.

---

## Triggering the Pipeline Manually

Since the platform frontend is not yet connected, trigger the pipeline by uploading the 1st JSON to S3:

```bash
aws s3 cp your_payload.json s3://thesamuraibucket/nameofpayload_output_$(date +%s).json
```

Jenkins polls the bucket and automatically picks up the latest file.

---

## Key Variables

| Variable | Description |
|---|---|
| `s3_payload_bucket` | S3 bucket containing the trigger payload |
| `s3_payload_key` | S3 key of the payload file |
| `aws_region` | AWS region (default: eu-central-1) |

---

## Requirements

- Terraform >= 1.4.0
- AWS Provider >= 6.2
- Ansible
- Jenkins with AWS credentials configured (`aws-terraform-creds`)
- GitHub token configured in Jenkins (`github-terraform-token`)
